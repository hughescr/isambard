{
  "name": "isambard",
  "version": "0.1.0",
  "description": "AI-powered Discord bot with persistent memory using DynamoDB",
  "type": "module",
  "main": "src/index.ts",
  "scripts": {
    "build": "bun build src/index.ts --outdir dist --target bun",
    "db:setup": "bun run scripts/migrate-db.ts",
    "dev": "bun --watch src/index.ts",
    "dev:docker": "docker compose -f docker/docker-compose.yml up --build",
    "lint": "eslint --cache --format overview .",
    "lint:fix": "eslint --cache --fix --format overview .",
    "mutate": "bunx stryker run",
    "sst-deploy": "if [ -z \"$AWS_ACCESS_KEY_ID\" ]; then op run --env-file=.env.op -- bunx sst deploy; else bunx sst deploy; fi",
    "sst-dev": "if [ -z \"$AWS_ACCESS_KEY_ID\" ]; then op run --env-file=.env.op -- bunx sst dev; else bunx sst dev; fi",
    "sst-diagnostics": "sh -c 'if [ -z \"$AWS_ACCESS_KEY_ID\" ]; then op run --env-file=.env.op -- bunx sst diagnostic --print-logs \"$@\"; else bunx sst diagnostics --print-logs \"$@\"; fi' _",
    "sst-remove": "if [ -z \"$AWS_ACCESS_KEY_ID\" ]; then op run --env-file=.env.op -- bunx sst remove; else bunx sst remove; fi",
    "test": "bun test",
    "test:full": "bun run lint;bun run typecheck;bun run mutate",
    "test:watch": "bun test --watch",
    "typecheck": "bunx tsc --noEmit"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "0.71.2",
    "@aws-sdk/client-dynamodb": "^3.700.0",
    "@aws-sdk/lib-dynamodb": "^3.700.0",
    "@hughescr/logger": "2.7.2",
    "discord.js": "^14.16.0",
    "zod": "4.2.1"
  },
  "devDependencies": {
    "@hughescr/eslint-config-default": "^4.0.0",
    "@stryker-mutator/core": "9.4.0",
    "@types/bun": "latest",
    "aws-sdk-client-mock": "4.1.0",
    "eslint": "^9.17.0",
    "eslint-formatter-overview": "2.0.0",
    "eslint-formatter-unix": "9.0.1",
    "eslint-plugin-package-json": "0.85.0",
    "sst": "latest",
    "testcontainers": "11.10.0",
    "typescript": "^5.7.0"
  }
}
